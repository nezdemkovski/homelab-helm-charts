{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "n8n.postgresql.fullname" . }}
  labels:
    {{- include "n8n.postgresql.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgresql
      protocol: TCP
      name: postgresql
  selector:
    {{- include "n8n.postgresql.selectorLabels" . | nindent 4 }}
{{- end }}
